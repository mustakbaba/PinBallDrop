namespace ElephantSocial.Chat.Model
{
    public enum ChatMessageType
    {
        TEXT,
        HELP,
        REWARD,
        JOIN_REQUEST,
        JOIN_ACCEPT,
        TEAM_UPDATES,
        JOIN_REJECT,
        KICK
    }
}